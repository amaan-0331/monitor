# Monitor Example

This is an example project that demonstrates how to use the `monitor` package.

## How to Run

1.  Navigate to the `example` directory:
    ```bash
    cd example
    ```
2.  Run the app:
    ```bash
    flutter run
    ```